export type Product = {
  id: string;
  name: string;
  price: number;
  category: string;
  sizes: string;
  colors: string;
};

export type Category = {
  id: string;
  name: string;
  description: string;
};
